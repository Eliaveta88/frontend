import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App boots without crash', (tester) async {
    // Placeholder — real tests will cover auth, routing, and features.
    expect(1 + 1, equals(2));
  });
}
