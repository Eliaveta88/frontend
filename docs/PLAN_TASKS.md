# План GastroRoute (актуально)

**Последнее обновление:** 2026-03-22 (фаза 3.2–3.3).

Кратко: **P1–P2 и продуктовый UI-план закрыты**; кнопки каталога/админа/финансов/логистики связаны с API; дашборд — **KPI рейсов в пути** считается по `total` с фильтром `status` на логистике.

---

## Сделано (срез)

| Область | Статус |
|--------|--------|
| Поиск, дашборд, ошибки Dio, маршрутизация, auth-флаги | ✅ |
| Склад — списки, оприходование | ✅ |
| Заказы — список с пагинацией, создание заказа | ✅ |
| Админ — список + регистрация пользователя | ✅ |
| Каталог — поиск, фильтр, **создание товара** | ✅ |
| Финансы — баланс, транзакции, **счёт и оплата** | ✅ |
| Логистика — список, **создание маршрута**, **фильтр списка по `status`** | ✅ |
| Дашборд — заказы сегодня (серверный `total`), **рейсы in_progress (`total` + `status=in_progress`)** | ✅ |
| Продуктовый UI: без Traefik/dev-текстов в интерфейсе | ✅ см. `PLAN_UI_PRODUCT.md` |
| **Фаза 2** — persist JWT, восстановление сессии, бизнес-API через `dioProvider` | ✅ |
| **Фаза 3.2–3.3** — выручка за день по API `GET .../revenue`; лента заказы+транзакции | ✅ |

---

## Фаза 1 — UX и качество фронта — сделано

| # | Задача | Статус |
|---|--------|--------|
| 1.1 | Скелетоны для списков/таблиц + дашборд и финансы | ✅ |
| 1.2 | `EmptyListState` | ✅ |
| 1.3 | `AsyncErrorCard` + `dioErrorMessage` | ✅ |

---

## Фаза 2 — Авторизация и сессия — сделано

**Цель:** готовность к «закрытому» API и нормальному UX после перезапуска приложения.

| # | Задача | Статус |
|---|--------|--------|
| 2.1 | Persist: access/refresh (web/mobile) | ✅ |
| 2.2 | Старт приложения: refresh профиля при наличии токена | ✅ |
| 2.3 | JWT на защищённых маршрутах: `dioProvider` вместо `rawDio` где требует бэкенд | ✅ |
| 2.4 | Документировать публичные vs защищённые пути в README | ✅ |

---

## Фаза 3 — Дашборд: честные KPI и активность — сделано

| # | Задача | Статус |
|---|--------|--------|
| 3.1 | Логистика: `routesInProgress` — **серверный `total` при `status=in_progress`** | ✅ |
| 3.2 | Финансы: выручка за день — **`GET .../accounts/{id}/revenue?from=&to=`** (сумма `completed` в интервале) | ✅ |
| 3.3 | Лента активности: **события из заказов + транзакций** (сортировка по времени), UI на дашборде | ✅ |

---

## Фаза 4 — Интеграция микросервисов (бэкенд)

**Цель:** убрать заглушки в данных; единые справочники.

| Сервис | Что сделать |
|--------|-------------|
| **orders** | Цены/имена из каталога; клиенты |
| **warehouse** | Названия товаров из каталога |
| **logistics** | Имя водителя из identity (сейчас из формы создания) |
| **finance** | **`credit_limit` и `status` из таблицы `accounts` в balance API**; полноценная генерация invoice — позже |
| **identity** | Надёжное хеширование паролей |

---

## Фаза 5 — Продукт и инфраструктура

| # | Задача | Статус |
|---|--------|--------|
| 5.1 | Кнопки каталога/админа/финансов/логистики | ✅ связано с API |
| 5.2 | Тесты (widget/integration), CI | ⏳ unit-тесты фронта; **CI** — `.github/workflows/ci.yml` в репозитории `GastroRoute_frontend` (analyze + test) |
| 5.3 | `git push` сабмодулей + bump в корневом репо | по необходимости |

---

## Флаги сборки

| Флаг | Назначение |
|------|------------|
| `API_BASE_URL` | Базовый URL API (на Web по умолчанию — `Uri.base.origin`) |
| `AUTH_ENABLED=true` | Обязательный вход, старт с `/login` |

## Git (сабмодуль + корень)

```bash
cd GastroRoute_frontend && git push
cd .. && git add GastroRoute_frontend && git commit -m "chore: bump GastroRoute_frontend" && git push
```

---

## Чеклист обновления этого файла

Переносите выполненное в **«Сделано»** и обновляйте статусы фаз 2–5.
