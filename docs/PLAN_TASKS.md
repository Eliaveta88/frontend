# План задач GastroRoute Frontend

Обновляйте статусы: `[ ]` → `[x]`. **Последнее обновление списка:** 2026-03-19.

## A — Поиск и дашборд (сделано)

- [x] Глобальный поиск в шапке → autocomplete каталога, переход к товару
- [x] Дашборд: KPI из каталога / заказов / финансов, pull-to-refresh
- [x] `dioErrorMessage` и обновление README

## B — Ошибки и документация (сделано)

- [x] Единый маппинг `DioException` → понятные тексты
- [x] Документация `API_BASE_URL`, таблица экранов

## C — Аутентификация и client_id (сделано)

- [x] Профиль пользователя (`UserProfile`) из ответа `login`
- [x] Синхронизация `financeClientIdProvider` с `user.id` при входе / сброс при выходе
- [x] `GoRouterRefreshNotifier` + редирект при `--dart-define=AUTH_ENABLED=true`
- [x] Меню в шапке: «Вошли как …»
- [x] Поле `client_id` на странице финансов подхватывает провайдер

## D — P1 склад и логистика (сделано)

- [x] **Склад:** `GET /warehouse/api/v1/warehouse` → `ApiPaths.warehouseStock`, `warehouseStockProvider`, таблица остатков
- [x] **Логистика:** `GET /logistics/api/v1/logistics` → `ApiPaths.logisticsRoutes`, `logisticsRoutesProvider`, таблица рейсов
- [x] **Дашборд:** KPI «Рейсов в пути» = число маршрутов `in_progress` (по первой странице списка)

## E — P2 (сделано)

- [x] **Заказы:** POST создание заказа, UI «Новый заказ», пагинация списка
- [x] **Заказы / KPI:** серверный фильтр `created_from` / `created_to` + `total` для «Заказы сегодня» на дашборде (не только первая страница)
- [x] **Склад:** POST оприходование (форма → API) + обновление остатков
- [x] **Админ:** список пользователей в `identity`, таблица на `/admin`
- [x] **Каталог:** локальный поиск и фильтр по категории по загруженному списку

---

## Что ещё сделать (приоритет)

### Только фронт / UX

| Приоритет | Задача |
|-----------|--------|
| P3 | **Скелетоны** и единый вид пустых списков |
| P3 | **JWT:** перевести вызовы на `dioProvider`, когда бэкенд потребует Bearer |
| P4 | **Дашборд:** лента активности (нужен источник событий) |
| P4 | **Persist:** токены + `refreshProfile` при старте |

### Бэкенд / контракт (по необходимости)

| Приоритет | Задача |
|-----------|--------|
| P3 | **Склад / логистика / финансы:** точные KPI без «первой страницы» (агрегаты или фильтры по дате, как в заказах) |
| P4 | **События** для ленты активности дашборда |

---

## Флаги сборки

| Флаг | Назначение |
|------|------------|
| `API_BASE_URL` | База Traefik (на Web по умолчанию — `Uri.base.origin`) |
| `AUTH_ENABLED=true` | Обязательный вход, старт с `/login` |

## Git (сабмодуль + корень)

```bash
cd GastroRoute_frontend && git push
cd .. && git add GastroRoute_frontend && git commit -m "chore: bump GastroRoute_frontend" && git push
```
