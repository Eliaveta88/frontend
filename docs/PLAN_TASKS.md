# План GastroRoute (актуально)

**Последнее обновление:** 2026-03-19.

Кратко: **P1–P2 по фронту закрыты** (заказы, склад, админ, каталог, KPI «заказы сегодня» считается на сервере). Ниже — **что ещё нужно** и **как идти дальше по фазам**.

---

## Сделано (срез)

| Область | Статус |
|--------|--------|
| Поиск, дашборд, ошибки Dio, маршрутизация, auth-флаги | ✅ |
| Склад / логистика — списки, склад — оприходование | ✅ |
| Заказы — список с пагинацией, создание заказа | ✅ |
| Админ — список пользователей из identity | ✅ |
| Каталог — локальный поиск и фильтр по категории | ✅ |
| Дашборд — «заказы сегодня» по `created_from` / `created_to` + `total` | ✅ |

---

## Фаза 1 — UX и качество фронта (без новых микросервисов) — сделано

**Цель:** единообразие ощущений при загрузке и пустых данных.

| # | Задача | Статус |
|---|--------|--------|
| 1.1 | **Скелетоны** для списков/таблиц (заказы, каталог, склад, логистика, админ) + дашборд и финансы | `loading_skeletons.dart` |
| 1.2 | **Единый пустой список** — `EmptyListState` | `empty_list_state.dart` |
| 1.3 | **Ошибки + retry** — `AsyncErrorCard` (`dioErrorMessage` внутри) | `async_error_card.dart` |

---

## Фаза 2 — Авторизация и сессия

**Цель:** готовность к «закрытому» API и нормальному UX после перезапуска приложения.

| # | Задача |
|---|--------|
| 2.1 | **Persist:** безопасное хранение access/refresh (web: подходящее хранилище; mobile: secure storage) |
| 2.2 | **Старт приложения:** при наличии токена — `refreshProfile` / `users/me`, восстановление сессии |
| 2.3 | **JWT на защищённых маршрутах:** перевести вызовы с `rawDio` на `dioProvider` там, где бэкенд требует Bearer (согласовать контракт по сервисам) |
| 2.4 | Развести **публичные** vs **только с токеном** пути в `ApiPaths` / клиентах (документировать в README) |

---

## Фаза 3 — Дашборд: честные KPI и активность

**Цель:** цифры не «с первой страницы», а по смыслу; лента — когда появится источник.

| # | Задача |
|---|--------|
| 3.1 | **Логистика:** `routesInProgress` — серверный фильтр/агрегат по `status` (аналогично заказам), фронт — один запрос с `total` |
| 3.2 | **Финансы:** выручка за день — фильтр по дате на API или агрегат `sum` за `completed` за день (сейчас частично обход по первым N транзакциям) |
| 3.3 | **Лента активности:** контракт событий (например `GET /events` или стрим) + UI на дашборде |

---

## Фаза 4 — Интеграция микросервисов (бэкенд)

**Цель:** убрать моки и заглушки из кода; единые справочники.

| Сервис | Что сделать (по коду/TODO) |
|--------|----------------------------|
| **orders** | Цены из каталога; имена клиентов; названия товаров из каталога |
| **warehouse** | Имя товара из каталога при оприходовании/остатках |
| **logistics** | Имя водителя из identity/users |
| **finance** | credit_limit из настроек счёта; генерация счётов (invoice) |
| **identity** | Нормальный хеш паролей (passlib и т.п.) |

---

## Фаза 5 — Продукт и инфраструктура

| # | Задача |
|---|--------|
| 5.1 | Кнопки-заглушки («Добавить товар», «Добавить сотрудника») — либо скрыть, либо связать с API |
| 5.2 | Тесты (widget/integration по критичным путям), CI |
| 5.3 | `git push` сабмодулей + bump в корневом репо (если используется) |

---

## Флаги сборки

| Флаг | Назначение |
|------|------------|
| `API_BASE_URL` | База Traefik (на Web по умолчанию — `Uri.base.origin`) |
| `AUTH_ENABLED=true` | Обязательный вход, старт с `/login` |

## Git (сабмодуль + корень)

```bash
cd GastroRoute_frontend && git push
cd .. && git add GastroRoute_frontend && git commit -m "chore: bump GastroRoute_frontend" && git push
```

---

## Чеклист обновления этого файла

По мере выполнения переносите пункты в **«Сделано»** или отмечайте фазы 1–5 выполненными.
