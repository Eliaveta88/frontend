# План: продуктовый UI без «dev»-текстов и рабочие кнопки

**Последнее обновление:** 2026-03-19.

## Текущий статус

| Блок | Состояние |
|------|-----------|
| **A** Копирайт и ошибки | ✅ Выполнено |
| **B** Кнопки (каталог, админ, финансы) | ✅ Выполнено |
| **C** Логистика «Новый рейс» | ✅ POST + диалог |
| **D** Дашборд, понятные подписи KPI | ✅ Выполнено |
| **Дашборд 3.1** Рейсы `in_progress` | ✅ Счётчик по `GET ...?status=in_progress` → поле `total` (не обход первой страницы) |

**Критерий A:** в `lib/features/**` нет пользовательских строк с Traefik / «сервисы запущены» / dev-команд Flutter.

---

## Часть A — Копирайт и ошибки (только фронт) — закрыто

| # | Задача | Где править |
|---|--------|-------------|
| A.1 | Подзаголовки экранов: нейтральные формулировки | `orders_page`, `warehouse_page`, `dashboard_page` |
| A.2 | Подсказка в `AsyncErrorCard` для заказов | `orders_page.dart` |
| A.3 | `dioErrorMapper`: нейтральные таймауты | `dio_error_mapper.dart` |
| A.4 | Пустые состояния / логистика | `logistics_page`, `warehouse_page` |
| A.5 | Не дублировать dev-комментарии в UI | — |

---

## Часть B — Кнопки с API — закрыто

- **B.1** Каталог — `POST` товар + диалог.  
- **B.2** Админ — регистрация пользователя + диалог.  
- **B.3** Финансы — счёт (`invoices/generate`).  
- **B.4** Финансы — оплата (`transactions`).

---

## Часть C — Логистика — закрыто

- **C.1** `POST /logistics/api/v1/logistics` (`PlanRouteRequest`), фронт: `LogisticsApiService.createRoute`, обновление списка.

---

## Часть D — Дашборд — закрыто (база)

- **D.1** Подписи KPI без технических деталей реализации.  
- **D.2** После появления агрегатов на бэкенде — обновить подписи (см. `docs/PLAN_TASKS.md`, фаза 3).

### Дополнение: KPI «Рейсы в пути»

- **Бэкенд:** `GET /logistics/api/v1/logistics?status=in_progress&skip=0&limit=1` — в ответе `total` = число маршрутов в работе.  
- **Фронт:** `dashboard_providers` — один запрос, без подсчёта по первым N строкам.

---

## Backlog (не в этом документе — детали в `PLAN_TASKS.md`)

- Авторизация: persist токенов, вызовы с Bearer по контракту.  
- Финансы на дашборде: выручка за день без обхода по N транзакциям (нужен агрегат или фильтр по дате на API).  
- Лента активности на дашборде.  
- Интеграции микросервисов (имена из справочников, счета и т.д.).

---

## Проверка

- [x] Нет пользовательских строк с Traefik / «сервисы запущены» / dev-команд Flutter.  
- [x] Видимые кнопки на основных экранах связаны с API или осознанно не показываются.  
- [x] Ошибки сети формулируются нейтрально.  
- [x] KPI «рейсы в пути» опирается на серверный `total` при фильтре по статусу.
