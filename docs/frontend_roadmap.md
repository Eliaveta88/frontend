# План доработки фронтенда (продолжение)

## Сделано (A + B + C)

- Глобальный поиск в шапке → autocomplete каталога, переход к `/catalog/:id`.
- Дашборд: KPI из API (каталог, заказы, финансы), pull-to-refresh, частичные ошибки.
- `dioErrorMessage` для единообразных текстов ошибок на ключевых экранах.
- **Авторизация:** `AuthState.profile`, синхронизация `financeClientIdProvider` с `user.id`, `GoRouterRefreshNotifier`, флаг `--dart-define=AUTH_ENABLED=true`.

Подробный чеклист: **`docs/PLAN_TASKS.md`**.

## Следующие шаги

### Итерация D — склад / логистика / админ

- [ ] Добавить `ApiPaths` + сервисы после появления REST в микросервисах.
- [ ] Заменить заглушки `DataTable` на данные из провайдеров.

### UX и инфра фронта

- [ ] Перевести защищённые запросы на `dioProvider` (Bearer), когда бэкенд начнёт требовать JWT.
- [ ] Скелетоны вместо спиннеров на списках.
- [ ] Лента активности на дашборде (события заказов/склада), когда будет API или WebSocket.
- [ ] (Опционально) Persist токенов + `refreshProfile` при старте приложения.
